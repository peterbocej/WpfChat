<Window x:Class="WpfChat.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfChat.Windows"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:WpfChat.ViewModel"
        Title="Login" Height="150" Width="300" WindowStartupLocation="CenterOwner">
   <Window.DataContext>
      <vm:LoginViewModel />
   </Window.DataContext>
    <Grid>
      <Grid.RowDefinitions>
         <RowDefinition/>
         <RowDefinition/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
         <ColumnDefinition Width="*"/>
         <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="User namme:" Grid.Column="0" Grid.Row="0" Width="100" Padding="20" VerticalAlignment="Center" />
      <TextBox x:Name="tbUserName" Text="{Binding UserName}" Grid.Column="1" Grid.Row="0" Width="120" Height="30" VerticalAlignment="Center" />
      <Button Content="Login!" Grid.ColumnSpan="2" Grid.Row="1" Width="100" Height="30" Command="{Binding LoginClickCommand}"/>
   </Grid>
</Window>
